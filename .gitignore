# atomic-write temp files (the service writes an mktemp'd .<day>.json.XXXXXX
# then renames it into place; older builds used <day>.json.part)
.*.json.*
*.part

# editor / OS noise
*.swp
*~
.DS_Store
.qmlls.ini
